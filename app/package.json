{
  "name": "inggrisinyuk-kids-app",
  "version": "0.1.0",
  "private": true,
  "description": "Prototipe TypeScript alur modul belajar (Vocabulary, Listening, Speaking, Grammar) - client-side murni, tanpa backend, dibundel jadi 1 file JS statis.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "verify:content": "node scripts/verify-vocab-content.mjs",
    "verify:duplicates": "node scripts/verify-content-duplicates.mjs",
    "build": "npm run typecheck && npm run verify:content && npm run verify:duplicates && esbuild src/main.ts --bundle --outfile=public/bundle.js --format=iife --target=es2019 --minify",
    "dev": "node dev-server.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
